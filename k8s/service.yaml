apiVersion: v1
kind: Service
metadata:
  name: bluegreen-service
spec:
  type: ClusterIP
  selector:
    app: bluegreen-app
    version: blue
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
